.body
  .header
    %h2= @user.screen_name
  .container-fluid
    .row
      .col-sm-3
        %p
          Users info here! this is where the users info goes!
        -if @user.comments.any?
          - @user.comments.each do | comment |
            = render partial: "shared/comment", locals: {comment: comment }
      .col-sm-7
        = render partial: 'user_article_feed'
